services:
  postgres:
    container_name: postgres
    image: postgres
    ports:
      - 5432:5432
    env_file:
      - ./envs/db.env
    volumes:
      - postgis_data:/var/lib/postgresql/data/
volumes:
  postgis_data: {}
